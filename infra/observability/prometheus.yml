global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: awa-backend
    metrics_path: /metrics
    static_configs:
      - targets: ["backend:8000"]

  # Add auction-worker, kafka-exporter, and node-exporter targets as those
  # components gain metrics endpoints in later sprints.
